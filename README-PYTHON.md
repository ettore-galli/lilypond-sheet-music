# PYTHON

Install by hand if needed only

## Install python + prerequisites

```shell

sudo apt-get update
sudo apt-get install python3

python3 -m venv .u-env

. .u-env/bin/activate

pip install -r requirements-ubuntu.txt

```
